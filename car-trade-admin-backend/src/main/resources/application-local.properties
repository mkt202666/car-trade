# ==================== Local Dev ====================
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=never

# Database - same as mobile backend
spring.datasource.url=${DB_URL:jdbc:postgresql://47.117.41.219:5432/new_car_trade?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&currentSchema=public}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:sxk#2025@}

# Redis - same as mobile backend
spring.data.redis.host=${REDIS_HOST:r-uf6t1iwa40jdn5oijmpd.redis.rds.aliyuncs.com}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:sxk@2025-Redis}

# JWT - shared secret via environment variable
# jwt.secret=${JWT_SECRET:}

# MyBatis-Plus SQL log
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# Logging
logging.level.root=INFO
logging.level.com.pancosky.cartradeadmin=DEBUG
logging.level.org.springframework.web=INFO
logging.level.io.lettuce.core=WARN
logging.level.com.zaxxer.hikari=INFO
logging.file.name=logs/admin-backend-local.log

# Demo mode - local only
app.demo-mode=true

# Login lock - disabled for local dev
security.login-lock.enabled=false
security.login-lock.max-fails=5
security.login-lock.lock-minutes=5
