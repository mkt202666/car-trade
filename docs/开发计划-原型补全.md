# 5D好车 - 原型功能补全开发计划

## 背景
通过分析线上原型 http://shengtaiprd.pancosky.com/5d/ 与当前项目代码对比，发现以下功能缺失或未完整注册。

## 开发目标
补全原型中存在但项目缺失的功能，确保前后端功能对齐度达到100%

## 开发周期
预计1周

---

## 阶段一：路由注册修复（0.5天）✅ 已完成

> 原型中已有页面，仅需注册路由即可使用

### 1.1 注册拍卖页面路由
- [x] 前端：`pages.json` 添加 `pages/auction/index` 路由
- [x] 前端：`pages.json` 添加 `pages/auction-detail/index` 路由
- [x] 验收：拍卖页面可通过路由正常访问

### 1.2 注册求购意向页面路由
- [x] 前端：`pages.json` 添加 `pages/purchase-demand/index` 路由
- [x] 验收：求购意向页面可通过路由正常访问

### 1.3 修复构建问题
- [x] 修复 `membership/index.vue` HTML entities 编码问题
- [x] 补充 `ai.js` 缺失的 API 函数（getMyCars, getMarketAnalysis, getCompetitors, getSuggestions, searchAllCars）
- [x] `npm run build:h5` 构建成功

---

## 阶段二：消息订阅管理（1天）✅ 已完成

> 原型消息Tab下有"消息|订阅"切换，订阅Tab包含通知类型开关

### 2.1 前端 - 消息订阅管理页面
- [x] 前端：消息中心页面添加 Tab 切换（消息 | 订阅）
- [x] 前端：订阅Tab 包含以下通知类型开关：
  - 自动推广结果
  - 订单状态变更
  - 合同签署提醒
  - 保证金变动
  - 车行动态
- [x] 前端：开关状态云端同步（GET/PUT `/messages/notification-settings`）
- [x] 验收：可切换消息/订阅Tab，开关状态正确保存

### 2.2 后端 - 订阅设置接口
- [x] 后端：`MessageController` 添加获取/更新订阅设置接口
- [x] 后端：数据库 `users` 表添加 `notification_settings JSONB` 字段
- [x] 后端：`schema.sql` 添加迁移脚本
- [x] 后端：`mvn compile` 编译通过
- [x] 验收：订阅设置可持久化存储

---

## 阶段三：求购意向功能完善（1天）✅ 已完成

> 原型支持发布求购意向，让车商主动匹配

### 3.1 前端 - 求购意向发布页面
- [x] 前端：求购意向表单包含：
  - 期望品牌/车系（级联选择）
  - 期望车型（多选）
  - 期望车况（非事故/事故、漆面、结构件等）
  - 期望价格区间
  - 期望车龄
  - 期望里程
  - 求购描述（textarea）
- [x] 前端：求购意向列表页面（我的求购）— `pages/my-purchase-demand/index.vue`
- [x] 前端：`pages.json` 注册路由
- [x] 前端：`pages/profile/index.vue` 添加"我的求购"入口
- [x] 验收：可发布求购意向并在列表中查看

### 3.2 后端 - 求购意向接口
- [x] 后端：`PurchaseDemandController` 完善 CRUD 接口
- [x] 后端：`PurchaseDemandService` 实现业务逻辑
- [x] 后端：`purchase_demands` 表结构确认（init.sql/schema.sql）
- [x] 验收：求购意向可创建、查询、修改、删除

---

## 阶段四：使用教程页面（0.5天）✅ 已完成

> 原型"我的"页有"使用教程"入口

### 4.1 前端 - 使用教程页面
- [x] 前端：创建 `pages/tutorial/index.vue`
- [x] 前端：教程内容分组展示：
  - 找车使用协议
  - 找车使用规范
  - 找车发车规范
  - 找车车源发布规范
  - 找车隐私政策
  - 交易规范
- [x] 前端：`pages.json` 注册路由
- [x] 前端：`pages/profile/index.vue` 已有"使用教程"入口
- [x] 前端：`npm run build:h5` 构建成功
- [x] 验收：可从我的页进入使用教程，查看各规范内容

---

## 阶段五：数据统计展示（1天）✅ 已完成

> 原型"我的"页展示车源数/被查看/沟通/粉丝及今日增量

### 5.1 前端 - 统计数据展示
- [x] 前端：`pages/profile/index.vue` 已有数据统计区域（stats-card）
- [x] 前端：展示内容：
  - 我的车源数（carCount）
  - 被查看次数（viewCount）+ 今日增量
  - 沟通次数（contactCount）+ 今日增量
  - 粉丝数（followerCount）+ 今日增量
- [x] 验收：统计数据正确展示

### 5.2 后端 - 统计数据接口
- [x] 后端：`UserController` 已有 `GET /users/me/stats` 接口
- [x] 后端：`UserService.getStats()` 实现统计数据聚合
- [x] 后端：从 `users` 表直接读取统计字段（onSaleCount, viewCount, messageCount, followerCount 等）
- [x] 验收：接口返回正确的统计数据

---

## 阶段六：分享与下载功能（0.5天）✅ 已完成

### 6.1 分享到微信
- [x] 前端：`pages/car-detail/index.vue` 已有分享按钮（导航栏、价格行、底部栏）
- [x] 前端：`shareCar()` 调用 `uni.showShareMenu` 分享到微信好友/朋友圈
- [x] 验收：可分享车源到微信

### 6.2 下载图片
- [x] 前端：`pages/car-detail/index.vue` 已有下载图片按钮
- [x] 前端：`downloadImages()` 调用 `uni.downloadFile` 下载车源图片
- [x] 前端：`downloadSingleImage()` 使用 `uni.saveImageToPhotosAlbum` 保存到相册
- [x] 前端：处理相册授权权限
- [x] 验收：可下载车源图片到手机相册

---

## 任务总览

| 阶段 | 功能 | 工作量 | 优先级 | 状态 |
|------|------|--------|--------|------|
| 一 | 路由注册修复（拍卖+求购意向） | 0.5天 | P0 | ✅ 已完成 |
| 二 | 消息订阅管理（Tab+开关+接口） | 1天 | P1 | ✅ 已完成 |
| 三 | 求购意向功能完善（发布+列表） | 1天 | P2 | ✅ 已完成 |
| 四 | 使用教程页面 | 0.5天 | P2 | ✅ 已完成 |
| 五 | 数据统计展示（车源/查看/沟通/粉丝） | 1天 | P2 | ✅ 已完成 |
| 六 | 分享与下载 | 0.5天 | P2 | ✅ 已完成 |
| **合计** | | **4.5天** | | **6/6 全部完成** |

---

## 验收标准

1. 所有页面可通过路由正常访问
2. 消息订阅设置可保存并生效
3. 求购意向可发布和管理
4. 使用教程内容完整展示
5. 统计数据正确展示
6. 分享和下载功能正常工作
7. `mvn compile` 编译通过
8. 前端 `npm run build:h5` 构建成功
